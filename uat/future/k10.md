# UAT: k10
Design: docs/design.md sections 3.5 and 6.5 (K10.1), 6.4 (R14); principles served: 10 (measured, then built), 1 (the disk is the truth)
Required units: K10
Fixture variant: clean; fault:unknown-usage for row 3 only
Platform/shell: macOS
Target phase: future
Row deadline: 90 per row (a deadline bounds a stuck test, it is not a latency claim)

1. Given a short completed turn against the local provider
   When I read [uat:usage-footer] on that turn and look beside it
   Then a separate element [uat:cache-qualifier] renders this exact sentence: "Cache reporting varies by provider and prompt size; a low reported share alone does not establish a prefix change."
   Shot: 01-short-local-turn.png
2. Given a longer tool-bearing fixture turn in the same session
   When I read [uat:usage-footer] and [uat:cache-qualifier] on that turn
   Then the qualifier shows the same sentence unchanged on every footer that is rendered, whatever the numbers on that footer say
   Shot: 02-long-fixture-turn.png
3. Given a turn whose usage record is unknown (fault fixture)
   When I read [uat:usage-footer] and [uat:cache-qualifier] on that turn
   Then the footer shows unknown values rather than invented zeros, and the qualifier still shows the same unconditional sentence
   Shot: 03-unknown-usage.png
4. Given the turns from rows 1 to 3
   When I inspect the snapshot's elements list for the turns
   Then [uat:cache-qualifier] is a distinct element from [uat:usage-footer] and from the unstable number segment; the number segment carries the unstable marker and no row in any script asserts its text
   Shot: none; Evidence: the snapshot elements list showing the separate ids per turn, and the qualifier text matched against the design sentence

Break attempts (notes only, each with a shot or log line): switch providers between turns and confirm the sentence never changes; send a zero-length session probe and confirm no footer is asserted when absent; compare two consecutive identical prompts and confirm no script expectation depends on the reported share.
