# UAT: k12
Design: docs/design.md sections 3.2, 3.5 and 6.5 (K12.1 to K12.3), 6.4 (R4.1, R5.1 to R6.3, R12); principles served: 3 (tokens have an owner), 6 (the board is the contract), 7 (work is attributable)
Required units: K12
Fixture variant: clean (section 6.2 fixture whose build ticket starts todo; named fault cases: a non-runnable board binary, a missing dated wiki-close pass)
Platform/shell: macOS
Target phase: future
Row deadline: 90 per row (a deadline bounds a stuck test, it is not a latency claim)

1. Given the build ticket still todo and the orchestrator chat ready
   When I ask through [uat:composer-input] for a child edit writing `result.txt` under that ticket's ID
   Then the turn refuses before execution with the `unticketed-action` refusal naming the tool, the ticket state todo and the `.pi/logs/session.jsonl` path; no child agent starts and `result.txt` does not exist
   Shot: 01-todo-refusal.png; Evidence: the tool-denial record in `.pi/logs/session.jsonl` and the absent `result.txt`
2. Given the same todo ticket with only the open-ticket pointer set on the tab
   When I repeat the same request in [uat:composer-input]
   Then the refusal stands unchanged: the pointer grants nothing, the database stays todo and no child executes
   Shot: 02-open-pointer-refusal.png; Evidence: the ticket's status row in `<project>/.pi/board.db` still todo
3. Given the ticket sheet open from a [uat:board-ticket] click
   When I read [uat:ticket-acceptance], click [uat:board-align] once, then click [uat:board-cancel]
   Then the sheet shows the workpad's deliverable and validation text; Cancel disarms the armed verb with no state change: the database stays todo and the sheet's status does not move
   Shot: 03-align-cancel.png; Evidence: the unchanged `<project>/.pi/board.db`
4. Given the sheet from row 3
   When I click [uat:board-align] once and then click the armed verb again (its visible `Align: click again` label)
   Then the sheet advances and the database says in_progress; a child can now start under this ticket
   Shot: 04-align-confirm.png; Evidence: `<project>/.pi/board.db` status in_progress after the second click only
5. Given the aligned ticket with recorded gates
   When I read the [uat:ticket-gate] rows and [uat:ticket-wiki-close] in the sheet
   Then each gate row shows its name, pass or fail, source and provider, timestamp and note; the wiki-close row shows its recorded verdict or the literal `no verdict recorded`
   Shot: 05-gate-rows.png
6. Given acceptance recorded as passing but no dated wiki-close entry yet
   When I read the sheet's verbs and [uat:ticket-wiki-close]
   Then [uat:board-close] is disabled or refuses visibly with the harness reason naming the missing wiki-close pass, the wiki-close verdict reads fail, and the ticket is not closed
   Shot: 06-close-refused.png; Evidence: `wiki/log.md` lacking the dated session entry and the failing gate record in `<project>/.pi/board.db`
7. Given an authorized worker has appended the dated wiki entry and `$HOME/Documents/Work/Lab/efficient-pi/bin/wiki-check` passes on the project
   When the wiki-close gate is recorded and I click [uat:board-close], then click the armed verb again (its visible `Close: click again` label)
   Then the human confirmation completes the close: the sheet and the database show the ticket closed
   Shot: 07-close-after-wiki.png; Evidence: the wiki-check output file named by the gate evidence and the close-check result
8. Given the Board view open via [uat:board-button] with its columns loaded, and the board command made to fail (fault: the configured board binary points at a non-runnable path)
   When I trigger a refresh
   Then [uat:board-error] appears reading `Refresh failed: <command>` and naming `.pi/logs/board.jsonl`; the previous columns stay visible marked `stale`; restoring the binary and refreshing clears both
   Shot: 08-board-error-stale.png; Evidence: `.pi/logs/board.jsonl` recording the failed command
9. Given the delegation turn completed with a tool-bearing fold
   When I click [uat:turn-fold] and read its [uat:action-row]
   Then the row displays role, agent, ticket, tokens, cost and duration ([uat:action-role], [uat:action-agent], [uat:action-ticket], [uat:action-tokens], [uat:action-cost], [uat:action-duration]) agreeing with the exact `actionId` record, and the visible totals equal the committed usage event without duplicating streamed updates
   Shot: 09-fold-action-rows.png; Evidence: the actions JSONL record for the opened `actionId`
10. Given a provider that accounts usage only per turn
    When I open the same fold and read [uat:action-tokens] and [uat:action-cost] on its rows
    Then per-action usage reads `not separately reported` and links the owning turn; nothing divides tokens equally and no per-action cost is invented; unknown stays unknown, never 0
    Shot: 10-not-separately-reported.png
11. Given the aligned ticket and the orchestrator delegated to write `result.txt`
    When I open the graph via [uat:graph-button] before delegating and during the run, then click the running [uat:graph-node-child]
    Then only [uat:graph-node-orchestrator] exists before delegation with no quiet-slot nodes; the child node shows running while it works and done when it finishes, and opening it shows the child's nonempty transcript ([uat:child-transcript])
    Shot: 11-child-lifecycle.png; Evidence: the children manifest record and the transcript file it names
12. Given the finished child from row 11
    When I switch to another tab and back, then quit and restart
    Then the same [uat:graph-node-child] persists as done and still opens the same nonempty transcript file; no node vanished and no new node appeared
    Shot: 12-node-persists.png
13. Given a child transcript open in its pane
    When I click `Return to chat` ([uat:child-return-chat]) and later `Open ticket` ([uat:child-open-ticket]) from that child
    Then Return to chat lands on the parent chat without losing the child tab, and Open ticket opens [uat:ticket-sheet] on exactly that child's ticket
    Shot: 13-child-navigation.png

Break attempts (notes only, each with a shot or log line): click Align twice within arming window and confirm exactly one status transition (db snapshot); confirm with `<project>/.pi/board.db` made read-only and read the action error shown in the sheet (shot); open the graph in a second tab while a child runs (shot); close the sheet while a verb is armed and confirm nothing fires (log line).
